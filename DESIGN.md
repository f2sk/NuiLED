# ホロフレ展示ケース（桃鈴ねね ソロライブ仕様）設計仕様書

## 1. 概要

ホロライブ公式ぬいぐるみ「ホロフレ」を安全に持ち運びつつ、ライブ会場や自宅で展示できる電飾付きディスプレイケースを製作する。

本作品は透明バッグを外装として利用し、内部に3Dプリント製フレームを組み込むことで、

- バッグの形状維持
- ジオラマ演出
- LEDライティング
- 電子制御
- メンテナンス性

を両立する。

---

# 2. システム構成

```
┌─────────────────────────┐
│        透明バッグ         │
│                         │
│  ┌──────────────────┐   │
│  │ 3Dプリントフレーム │   │
│  │                  │   │
│  │  背景            │   │
│  │                  │   │
│  │     ホロフレ      │   │
│  │                  │   │
│  │      台座         │   │
│  └──────────────────┘   │
│                         │
└─────────────────────────┘
```

---

# 3. バッグ仕様

|項目|寸法|
|---|---:|
|内幅|170 mm|
|内高さ|235 mm|
|内奥行|110 mm|

バッグは保護ケースとして使用し、構造強度は内部フレームで確保する。

---

# 4. 3Dプリント構造

## 全体方針：エッジフレーム（ロールケージ）＋パネル

直方体の12辺を骨格とする**エッジフレーム**を主構造とし、パネルは必要部（背面・底）だけ持たせる。

- 3D剛性が高くねじれに強い（平パネル単体より箱形状が安定＝バッグ形状維持に有利）
- 細い直線ストラット＋コーナーで構成でき、A1 miniで印刷しやすい
- 樹脂・重量減、中身のぬいが見えやすい（透明バッグ展示向き）
- ストラットを中空/溝付きにしてLED配線を辺の中に隠せる（§12「配線は極力見えない」）

構成：**エッジフレーム（12辺＋8コーナー）／溝スライド背面パネル／2重デッキ底（電源内蔵）／前面フレーム**

## 使用プリンタ / 造形サイズ

Bambu Lab A1 mini、造形可能 180×180×180 mm。全部品を上記内で印刷可能な構成とする（170/110の辺は一発、235の辺・背面パネルは分割）。

## エッジフレーム（12辺＋8コーナー）

バッグ内寸 170(W)×235(H)×110(D) の直方体の辺で構成。

- 辺：幅170×4／奥行110×4／高さ235×4。170・110は一発印刷、**235の縦辺は1分割**（各≈118mm）
- **コーナー＝8個の3方向ブラケットが要**。ケージ剛性はここで決まるので**肉厚厚め・フィレット/ガセット**で稼ぐ（過去のパネル継手割れ＝薄肉＋印刷ダレの教訓を反映）
- ストラット断面は**三角断面＋奥行き方向に厚み**で、前面投影（見付け）を増やさず剛性を確保。内側面にLEDストリップを貼る
- 辺は中空/溝としてLED配線を通し隠す

## 溝スライド背面パネル

- サイズ 170×235 mm（バッグ内寸に一致）。A1 miniを超えるため**高さ235側を2分割**（各170×117.5mm、実機検証済み）。「もう片側170mmは170を超えない」を分割制約とする
- **背面4辺の内側に溝（レベット）を切り、上からスライド差し込み＝交換式**（背景画像を貼り替え可能、§12）
- 荷重はエッジフレームが担うため**背景パネルは非構造材＝薄く軽くできる**（旧・パネル自身で剛性を持たせて継手が割れた問題を回避）
- 背景画像は印刷物を貼付。Back LEDが照らす

## 2重デッキ底（電源内蔵）

下板＋天板＋側壁（側壁はエッジフレーム底4辺と兼用）で浅い箱を作り、内部に電源・回路を収納。

- **収納物**：市販モバイルバッテリー＋制御基板（両面Cタイプ）＋XH端子・配線
- **天板＝台座/ジオラマの床**。天板の通し穴からLED配線をエッジフレームの辺へ通す（配線隠し）
- **側面アクセス**：側壁にハッチ/開口を設け、**モバイルバッテリーのボタン・充電ポート・XIAOのUSB-C**に触れる（起動・充電・書き込み）。バッテリーは抜き差し可能とする
- **高さ配分**：内高235 ＝ 底デッキ空洞（バッテリー厚が律速 ≈20〜25mm）＋板厚＋ぬい展示ゾーン（≈200mm）＋上部クリアランス
- **放熱スリット必須**：密閉空洞に電池＋2.5A回路＝発熱源。側壁にスリット、電池上を完全密閉しない（熱・リチウム安全）

## 前面フレーム

エッジフレームの**前面4辺**（既存 nuiHolder v20 相当、三角断面R処理＋奥行き補強）。

- 役割：前面LED保持／ケース剛性／ホロフレ照明

## ホロフレ固定

透明テグス等で固定（衣装への負荷を最小限）。エッジフレーム上辺をアンカーに吊る形も可。

---

# 5. LEDシステム

## LED

WS2812B

幅：6 mm

---

## 構成

### Front LED

用途

ホロフレ照明

役割

- 顔照明
- 衣装照明
- 撮影用照明

暖色系を基本とする。

---

### Back LED

用途

背景演出

役割

- 背景ライトアップ
- 雰囲気演出
- アニメーション

オレンジ・黄色を基本色とする。

---

## LED配置

### Front

ケース前面フレーム四辺

### Back

背景板外周

---

## 制御

Front / Back を独立制御可能とする。

LEDごとの個別制御により、

- 明るさ
- 色
- エフェクト

を自由に設定可能とする。

---

# 6. マイコン

## 候補

ESP32-S3

### 要件

- Bluetooth LE
- Web Bluetooth対応
- FastLED対応
- USB Type-C
- OTA更新対応（将来）

---

# 7. 操作

## 操作の切り分け

- **タクトSW（単体運用）**：スマホ無しで日常操作が完結する。ON/OFF・輝度・プリセット順送り
- **スマホ（BLE）**：任意色・演出パラメータの微調整、プリセットの編集/保存（作り込み用）

## 本体操作（外付けタクトスイッチ ×2）

XIAOオンボードのBOOTボタン（GPIO0）はストラップピン＆基板上SMDで操作性・起動安全性に難があるため使わない。**ケース外面からアクセスできる外付けタクト2個**（SW1=D3/GPIO4, SW2=D4/GPIO5、内部プルアップ）で操作する。

| 操作 | 機能 |
|---|---|
| SW1 短押し | 輝度サイクル（30→50→70→100%→循環、2.5A内） |
| SW1 長押し（約1s） | 全体ON/OFF（P-MOS母線トグル＝消灯/復帰） |
| SW2 短押し | プリセット順送り（P1→P2→P3→P4→循環） |
| SW2 長押し（約1s） | Front/Back 個別ON/OFF トグル |

## プリセット（4種、初期）

1. 暖白のみ（Front照明のみ、Back消灯）
2. 暖白＋桃橙ゆらぎ（Frontは暖白、Backはピンク/オレンジのブレス）
3. 桃橙ウェーブ（Back主体のウェーブ演出）
4. チェイス（流れる演出）

## 状態の永続化

最後の**輝度・プリセット・ON/OFF状態・Front/Back個別状態**をNVS（ESP32内蔵Flash）に保存し、次回起動時に復元する。単体運用で「電源を入れたら前回の状態で点灯」を実現する。

---

# 8. スマートフォン連携

## 通信方式

Bluetooth Low Energy

Chrome Web Bluetooth API を利用する。

専用アプリは作成しない。

---

## スマホUIの用途（2分法）

- **① プリセット作成・転送**：常時通信不要、散発的なRead/Write。プリセットの編集・転送・NVS保存。→ **段階2で実装済み**
- **② 演出・音楽同期**：リアルタイム直接制御（Write Without Response/Notifyを高頻度）。→ 今は不要、GATTに枠だけ予約

①②は同じGATTサービス内の別キャラクタリスティックとして共存。①を作り込んでも②は後からストリーム用キャラを生かすだけで拡張可能。

## GATTプロトコル（段階2実装）

Service UUID: `a1b20001-5c3d-4e6f-8a90-1234567890ab`

| キャラ | UUID末尾 | 属性 | 内容 |
|---|---|---|---|
| State | ...0002 | R/Notify | `[powerOn, brightnessIdx, activeSlot, channelMask, numSlots]`（5byte）。状態変化でNotify |
| SlotSelect | ...0003 | R/W | 編集対象スロット番号（0..7） |
| PresetData | ...0004 | R/W | 選択スロットの Preset(47byte) を読み書き |
| Command | ...0005 | W | オペコード（下記） |
| Realtime | ...0006 | W(no-rsp) | **②用に予約**（現状スタブ） |

Preset(70byte) = Front + Back + Ground の3チャンネル＋flags。各チャンネルは共通の演出エンジン：

- **ChannelPreset(23byte)**: `effect, speed, period, brightness, numColors, palette[RGB×6]`
- **Preset**: `front(23) + back(23) + ground(23) + flags(1)`（flags bit0=Front, bit1=Back, bit2=Ground）
- **Effect**: 0=単色 / 1=ブレス / 2=交互(周期＝ブロック長) / 3=ウェーブ / 4=チェイス
- **明るさ**: チャンネル毎の brightness × グローバルマスタ（SW1/BLE）
- パレット最大6色（`MAX_COLORS`、可変）

**ゾーン構成**（物理2データ線を3ゾーンへ）：各線60球のうち根本 `GROUND_LEN=17` 球が床。
- Ground = 線A床[0..16]＋線B床[0..16]（34球を連続扱い）
- Front / Back = 各線の壁[17..59]（43球）。`SWAP_WALL` で線A/線Bの壁割当を入れ替え（コネクタ逆対応）
- チャンネルマスク: CH_FRONT=1 / CH_BACK=2 / CH_GROUND=4

**チャンネル選択はプリセットから独立したグローバル設定**：どのゾーンを表示するかは `channelMask`（SW2長押し／BLE CHANNEL／Web操作パネルのトグル）で決め、プリセット適用時にリセットしない。プリセットは常に3ゾーンの中身を持つ（`flags` はレイアウト保持のため残すが表示判定には使わず、renderは `channelMask` のみで判定）。→ 「チャンネルを絞っただけの冗長プリセット」が不要になる。

Commandオペコード: `0x01`=APPLY(スロット適用) / `0x02`=SAVE(全スロットNVS保存) / `0x03`=FACTORY(工場出荷) / `0x05`=POWER / `0x06`=BRIGHTNESS(idx) / `0x07`=CHANNEL(mask)

①の操作フロー：接続 → SlotSelect書込 → PresetData読/書 → Command 0x02(SAVE) → 切断

## Web UI

Chrome Web Bluetooth API を利用（専用アプリなし）。GitHub Pagesで配信（`docs/`）。

- **オフライン優先**：プリセットはブラウザの localStorage にライブラリとして保持。**未接続でも作成・編集・保存が可能**
- **型ベース編集**：Front/Back 各チャンネルで 演出（単色/ブレス/交互/ウェーブ/チェイス）・明るさ・速度・周期・パレット（フルRGB、最大6色）を編集
- **転送**：接続時のみ、編集中プリセットを本体スロットへ書込＋適用、NVS保存、本体スロットの取込が可能
- 無装飾のニュートラルなデザイン

---

# 9. LED演出

## 標準演出

- 常時点灯
- ブレス
- フェード
- ウェーブ
- チェイス

---

## 起動演出

例

```
Front LED
    ↓
ゆっくり点灯

↓

Back LED
演出開始
```

---

# 10. 音楽同期（将来実装）

スマートフォン側HTMLプレイヤーを利用する。

```
MP3

↓

HTML Audio

↓

Timeline

↓

BLE

↓

ESP32

↓

LED
```

HTML側で

- 音楽再生
- タイムライン管理
- BLE通信

を担当する。

ESP32はLED制御のみ担当する。

---

# 11. 電源

USB給電

モバイルバッテリー使用を前提とする。**バッテリーは2重デッキ底に内蔵し、側面ハッチからボタン・充電ポート・XIAOのUSB-Cにアクセス**する（§4「2重デッキ底」参照）。

## LED電源制御

LED母線（5V）への給電は **P-ch MOSFET** で断続する。

- マイコン（ESP32-S3）のGPIOからP-MOSのゲートをドライブしON/OFF
- ESP32-S3は3.3V系のため、ゲート駆動はNPN/N-MOS等でのレベル変換を挟む（3.3V→ゲート−5V確保）
- 消灯時はLED母線を完全に遮断し、待機電流を最小化する

## 放熱方針

常時最大点灯は前提としない。

- 点灯時間・輝度に運用上の縛りを設ける
- 熱的に成立する範囲で実用する（密閉バッグ内の熱こもりを考慮）

---

# 12. 設計方針

- バッグは保護ケースとして扱う
- 内部フレームのみで自立可能とする
- A1 miniのみで全パーツを造形可能とする
- 電子回路は底板へ集約する
- 配線は極力見えない構造とする
- 背景画像は交換可能な構造を目指す
- LED演出と照明を完全に分離する
- 専用アプリは作らず、Chromeのみで操作可能とする
- 将来的な機能追加を考慮し、ソフトウェア・ハードウェアとも拡張性を確保する

---

# 13. 今後の検討事項

## ハードウェア

- MCU選定（ESP32-S3候補）
- バッテリー容量
- USB-C充電方式
- LED本数最適化
- 放熱対策
- 背景板固定方式
- 背景交換機構
- ホロフレ固定方法
- ケース内配線ルート

## ソフトウェア

- BLE通信プロトコル設計
- Web UIデザイン
- プリセット保存機能
- エフェクトライブラリ
- 音楽同期機能
- タイムラインエディタ
- OTAアップデート
- ログ・デバッグ機能

---

# 14. 制御回路 詳細設計（確定版）

## 14.1 設計前提

- 用途：ライブ会場前後の展示がメイン（短時間・断続運用、長時間連続点灯は不要）
- 電源：USB-C 5V / 3A（モバイルバッテリー、非PD）を基準
- LED総電流は **2.5Aで上限クランプ**（FastLED `setMaxPowerInVoltsAndMilliamps(5, 2500)`）
- 実運用は暖色白・桃橙中心＋非最大輝度のため **1〜2A** に収まる
- LEDは手持ちの **WS2812B 60球/m × 2本** を使用

## 14.2 光源イメージ

- **Front**：やや暖色寄りの白（顔照明・撮影用）
- **Back**：ピンク＋オレンジ混合の背景演出
- （将来案）Frontのみ SK6812 RGBW 化で暖色白の色再現・効率を改善する余地あり

## 14.3 構成部品選定

| 機能 | 選定 | 理由 |
|---|---|---|
| MCU | XIAO ESP32-S3 | USB-C・3.3V LDO・BLE内蔵、小型で底板に載せやすい |
| データレベル変換 | TC74AC125P（DIP-14） | 手ハンダ容易。VCCをダイオードで≈4.4Vに下げ、AC入力しきい値(0.7×VCC)を落として3.3V入力を確実にHと読ませる（VCCシフト運用） |
| 母線スイッチ | 単一 P-ch MOSFET（MTP4835I3） | 全5V母線を一括ON/OFF。消灯時の待機電流を遮断 |
| ゲート駆動 | N-ch small（2N7002/2N7000） | ハイサイドPchを0⇔5Vで駆動 |
| 突入抑制 | 固定RCゲートソフトスタート | 母線には直列R入れず、ゲートをなまして dV/dt を抑制 |

## 14.4 ピンアサイン（XIAO ESP32-S3）

| 信号 | ピン | 設定 |
|---|---|---|
| Front Data | D0 (GPIO1) | 出力 → TC74AC125P |
| Back Data | D1 (GPIO2) | 出力 → TC74AC125P |
| 母線EN（P-MOS駆動） | D2 (GPIO3) | 出力（H=点灯） |
| SW1 | D3 (GPIO4) | 入力・内部プルアップ |
| SW2 | D4 (GPIO5) | 入力・内部プルアップ |

## 14.5 結線図

```
USB-C(5V/3A)─┬─→ XIAO 5V pin ──┐   ← 入口はXIAO一本（2.5Aクランプ前提で直接供給）
             │                 ├─ Cbulk 1000µF ─ GND
             └─→ P-MOS(S) ──────┘
                    │(D)                    ← Drain = スイッチ後5V母線
                    ├─[1N4148]─→ AC125 VCC (≈4.4V, +100nF)  ← VCCシフト
                    ├──→ Front WS2812B +5V （両端給電, 手前に470µF）
                    └──→ Back  WS2812B +5V （両端給電, 手前に470µF）

 P-MOSゲート: S(5V)─Rpu10k─G ─Rg1k─ [2N7000 D]   Cgs(G-S)≈2.2〜10µF
                                       [2N7000 G]←GPIO(D2, 母線EN)
                                       [2N7000 S]─GND
   → ONでゲートが Rg×Cgs でゆっくり降下＝突入抑制、定常はゲート0Vで全開

 データ: GPIO(D0)→AC125 in→out ─330Ω→ Front Din
        GPIO(D1)→AC125 in→out ─330Ω→ Back  Din
   AC125: 使用2回路の/OE=L, 未使用2回路は in=GND・/OE=H
```

**重要な設計判断**
- **レベル変換はVCCシフトで成立させる**：TC74AC125Pの入力しきい値は VIH=0.7×VCC。VCCを5.0Vのままだと3.5V＞3.3Vで規格上ギリギリ下。ダイオード1本（1N4148, Vf≈0.6V）でAC125のVCCを≈4.4Vへ落とすと VIH≈3.08V＜3.3V で確実にHと読む。出力Highも≈4.4V＞WS2812BのVIH(3.5V)で十分。DIPの楽な手ハンダのまま規格内に収まる。
- AC125（＋VCCシフトのダイオード）は**スイッチ後の5V母線から給電**する。消灯時にデータ線も同時に落ち、WS2812BのVCC断時データ流入（保護ダイオード経由の素子ダメージ）を原理的に回避する。
- 突入対策のR・Cは**ゲート側のみ**。母線に直列抵抗は入れない（3A×R の損失・電圧降下を避ける）。変動抵抗の役割はMOS本体が担い、ゲートRCはその開き方の速さだけを管理する。
- 起動シーケンス：XIAOがUSB給電で先にブート → GPIOで母線EN → 母線＋AC125起動 → データ送出。
- 動作NG時（先頭LEDちらつき/色化け）の切り分け：非破壊で即検出できる。最終手段としてTC74VHCT541AFT（TSSOP・要変換基板）へ差し替える余地はあるが、VCCシフトで通常は不要。

### 結線リスト（ピンネット・現物配線用）

**同じネットに並ぶピンは全て接続する**（＝足同士を繋ぐ対応表）。

**ノード呼称**
- **Vusb**：USB由来の非スイッチ5V（＝XIAO 5Vピン＝Smtp）
- **Vmtp**：MTP4835のDから出るスイッチ後母線（＝Dmtp）
- **Vac**：AC125のVCC（≈4.4V、1N4148の後）
- **Gmtp**：MTP4835ゲートノード（旧PGATE）

**部品ピン参照**
- **XIAO ESP32-S3**（シルク印字ラベル）: D0=Front Data / D1=Back Data / D2=母線EN / D3=SW1 / D4=SW2 / 5V=Vusb / GND=共通 / 3V3=未使用
- **MTP4835I3(TO-251)**: **Gmtp** / **Dmtp**(=タブ=Vmtp) / **Smtp**(=Vusb) ※脚順はデータシートで要確認
- **2N7000(TO-92, 平面手前・脚下 左→右 1-2-3)**: 1=**S2n** 2=**G2n** 3=**D2n**
- **TC74AC125P(DIP-14)**: 1=1OE 2=1A 3=1Y 4=2OE 5=2A 6=2Y 7=GND 8=3Y 9=3A 10=3OE 11=4Y 12=4A 13=4OE 14=VCC
- **1N4148**: A(アノード) / K(カソード=帯側)

**受動部品（両端の接続先）**
| 部品 | 値 | 一端 | 他端 |
|---|---|---|---|
| R1 | 10k | Vusb | Gmtp |
| R2 | 1k | Gmtp | D2n |
| R3 | 330Ω | AC125:3(1Y) | Front Din |
| R4 | 330Ω | AC125:6(2Y) | Back Din |
| C1 | 2.2〜10µF | Gmtp | Vusb |
| C2 | 1000µF(＋) | Vmtp | GND(−) |
| C3 | 470µF(＋) | Vmtp(Front給電点) | GND(−) |
| C4 | 470µF(＋) | Vmtp(Back給電点) | GND(−) |
| C5 | 100nF | Vac | GND |

**ネット別メンバー**
| ネット | 接続するピン |
|---|---|
| **Vusb** | XIAO:5V / Smtp / R1 / C1 |
| **Vmtp** | Dmtp(タブ) / 1N4148:A / C2:＋ / C3:＋ / C4:＋ / Front XH:5V / Back XH:5V |
| **Vac** | 1N4148:K / AC125:14(VCC) / AC125:10(3OE無効) / AC125:13(4OE無効) / C5 |
| **Gmtp** | Gmtp / R1 / C1 / R2 |
| **D2n** | D2n / R2 |
| **BUS_EN** | XIAO:D2 / G2n |
| **F_MCU** | XIAO:D0 / AC125:2(1A) |
| **F_OUT** | AC125:3(1Y) / R3 |
| **F_DIN** | R3 / Front XH:Data |
| **B_MCU** | XIAO:D1 / AC125:5(2A) |
| **B_OUT** | AC125:6(2Y) / R4 |
| **B_DIN** | R4 / Back XH:Data |
| **SW1_IN** | XIAO:D3 / SW1（他端=GND） |
| **SW2_IN** | XIAO:D4 / SW2（他端=GND） |
| **GND** | XIAO:GND / S2n / AC125:1(1OE有効) / AC125:4(2OE有効) / AC125:7(GND) / AC125:9(3A) / AC125:12(4A) / C2:− / C3:− / C4:− / C5 / SW1 / SW2 / Front XH:GND / Back XH:GND |

未接続で放置：AC125:8(3Y), 11(4Y)（未使用出力はオープン）。

ゲート駆動まわり：
```
 Vusb ──[R1 10k]──── Gmtp        （既定でゲートを5Vへ＝P-MOS OFF）
 Vusb ──[C1 2.2〜10µF]── Gmtp    （Cgs、ソフトスタート）
 Gmtp ──[R2 1k]── D2n
 G2n ── XIAO:D2(BUS_EN)     S2n ── GND
```
D2=Hで2N7000 ON→Gmtpを引き下げ→母線(Vmtp)ON。立ち上がりはR2×C1で緩やか＝突入抑制。

配線注意：
- **AC125のOE**：1OE(1)・2OE(4)を**GND**に落として出力有効（74x125はアクティブLow）。未使用側3OE(10)・4OE(13)は**Vac**へ上げて無効
- **データ直列330Ω**：AC125:3→R3→Front Din、AC125:6→R4→Back Din（直結でなくR3/R4を挟む）
- **XH極性**：5V/GND逆接でWS2812B即死。DataはテープのDin側へ
- **大電流ネット（Vusb / Vmtp / GND）**：2.5Aが流れる。スズメッキ線で太短く、XIAOの5V/GNDは直ハンダ
- **電解C極性**：C2/C3/C4は＋をVmtp・−をGNDへ。C5(100nF)は無極性
- **MTP4835の脚順**（Gmtp/Dmtp/Smtp）はデータシートで確認

## 14.6 電流収支（120球）

| 輝度 | Front(暖白60) | Back(桃橙60) | +MCU | 合計 |
|---|--:|--:|--:|--:|
| 100% | 〜2.0A | 〜1.6A | 0.2 | 〜3.8A → 2.5Aクランプで頭打ち |
| 50% | 〜1.0A | 〜0.8A | 0.2 | 〜2.0A |
| 30% | 〜0.6A | 〜0.5A | 0.2 | 〜1.3A |

- モバイルバッテリーの5V出力（USB-A 2.4A / USB-C 3A）ではフル白7.2Aは供給不可 → **ファームクランプが必須かつ最適**。
- 突入電流でバッテリー保護停止する恐れ → ゲートRCソフトスタートで回避。

## 14.7 部品リスト（BOM：パスA 手ハンダ／秋月調達）

LEDとチップRCは手元品を流用するため購入リストから除外（値は下表・実装欄に記載）。

**購入（秋月電子）**
| 記号 | 部品 | 秋月 通販コード / URL | 価格(税込) | 備考 |
|---|---|---|--:|---|
| U1 | XIAO ESP32-S3（無印） | [118078](https://akizukidenshi.com/catalog/g/g118078/) | 要確認(≈1,900) | Sense版(118079)ではない方 |
| U2 | レベル変換 TC74AC125P | [111487](https://akizukidenshi.com/catalog/g/g111487/) | ¥30 | DIP-14。VCCシフトで使用 |
| Q1 | Pch MOSFET MTP4835I3 | [116096](https://akizukidenshi.com/catalog/g/g116096/) | ¥40 | 30V/40A/17mΩ, TO-251S。母線SW |
| Q2 | Nch MOSFET 2N7000 | [109723](https://akizukidenshi.com/catalog/g/g109723/) | ¥30 | ゲート駆動段, TO-92 |
| D1 | 小信号ダイオード 1N4148 | [検索](https://akizukidenshi.com/catalog/goods/search.aspx?keyword=1N4148) | 数円 | AC125のVCCシフト用（手持ちあれば不要） |
| C2 | 電解コン 1000µF | [検索](https://akizukidenshi.com/catalog/goods/search.aspx?keyword=1000uF) | 数十円 | 母線バルク（手持ちに大容量無ければ） |
| C3,C4 | 電解コン 470µF ×2 | [検索](https://akizukidenshi.com/catalog/goods/search.aspx?keyword=470uF) | 数十円 | テープ手前バルク |
| SW1,SW2 | タクトスイッチ ×2 | [検索](https://akizukidenshi.com/catalog/goods/search.aspx?keyword=%E3%82%BF%E3%82%AF%E3%83%88%E3%82%B9%E3%82%A4%E3%83%83%E3%83%81) | 数十円 | 好みの品 |
| — | 両面ユニバーサル基板 Cタイプ 72×47mm | [103231](https://akizukidenshi.com/catalog/g/g103231/) | 要確認 | 両面スルーホール・めっき仕上げ |
| J1,J2 | JST XH 3P 横出しベース S3B-XH-A | [112263](https://akizukidenshi.com/catalog/g/g112263/) | ¥15 | Front/Back LED接続 ×2（3A定格） |
| — | JST XH 3P ハウジング XHP-3 | [112256](https://akizukidenshi.com/catalog/g/g112256/) | ¥5 | ×2（テープ側） |
| — | XHコンタクト SXH-001T-P0.6（10個） | [112264](https://akizukidenshi.com/catalog/g/g112264/) | ¥30 | 圧着（自前）。AWG28-22対応 |
| — | ピンヘッダ・スズメッキ線 | [検索](https://akizukidenshi.com/catalog/goods/search.aspx?keyword=%E3%83%94%E3%83%B3%E3%83%98%E3%83%83%E3%83%80) | 適宜 | XIAO実装・5V/GND母線 |

**手元流用（購入不要）**
| 記号 | 部品 | 定数 | 備考 |
|---|---|---|---|
| R1 | ゲートプルアップ | 10k | G→5V、既定OFF |
| R2 | ゲート直列 | 1k | Cgsと合わせ〜10ms |
| C1 | ゲートソフトスタート | 2.2〜10µF | ランプ時間調整（要実測） |
| R3,R4 | データ直列 | 330Ω ×2 | AC125出力 |
| C5 | AC125デカップリング | 100nF | VCC直近 |

## 14.8 製作方針

- **パスA（手ハンダ／ユニバーサル基板）**：試作・動作確認用。DIP/スルーホール部品中心で組む。USB-C入口はXIAO一本（下記14.9）。
- **パスB（JLCPCBで基板起こし）**：完成品として1枚に清書する場合。USB-Cコネクタ直付け・SMD小型化・3Aパターン自由。
- 定石：**パスAで試作 → 固まったらパスBで清書**。

## 14.9 USB-C入口＝XIAO直接供給（確定）

- 総電流を2.5Aでクランプする前提なら、専用USB-Cブレークアウトは不要。**入口はXIAOのUSB-C一本**とする（配線最短・部品最少）。
- 実運用1〜2A・ピーク2.5A・断続使用のため、XIAOの5Vピン経由でも実用範囲。

### XIAO電源経路の実態（回路図確認済み）
- **5Vピン＝USB-C VBUSの直タップ**。充電IC（U4=SGM40567-4.2）と3.3Vレギュレータ（U3=SGM6029）はVBUSから並列にぶら下がる枝で、5Vパッドとの間に直列で入っていない。よって**5Vピンから引く電流はどのICも通らず、直列のヒューズ/ダイオード/PMICも無い**（Seeedが「逆給電時は自分でダイオードを」と注意するのは直結だから）。
- したがって律速は「IC電流制限」ではなく**機械・熱**：USB-Cレセプタクル（USB-C規格3A）／基板内VBUSパターン（短距離）／ヘッダ・ソケット接点。公式に最大電流値が無いのは直列の電流制限素子が無いため。

### 実装（ガラエポ基板・確定方針）
- ベースは**両面スルーホール Cタイプ（72×47mm）**。**XIAOはピンヘッダで直ハンダ固定**（着脱ソケットにしない＝接点ホットスポットを作らない）。両面なのでGND母線・ジャンパが楽。
- **LEDテープ接続はJST XH 3P 横出し（S3B-XH-A）×2**（Front/Back）。裸線ピッグテールはXHコンタクト圧着でハウジング化。横出しでケーブルが上方向に飛ばず薄型ケースに収まる。**5V/GND逆接はWS2812B即死**なのでシルク/ラベルで極性明記、接続はテープのDin側端へ。XIAOのUSB-Cは基板の別辺に出して給電・書き込みを確保。
- LED母線の5V/GNDは**XIAOの5V/GNDパッド（ヘッダ足）から直ハンダで引き出し、スズメッキ線で取り回す**。φ0.6mm以上なら数A余裕。
- 注意：LED電流2.5Aは**5Vピン1本・GNDピン1本**（XIAOは各1ピンのみ）を通る。2.54mmヘッダピン定格≈3A/ピンで範囲内だが最細部。ハンダはたっぷり盛り、5V/GNDは太短く、GNDはスター的に戻す。
- 内部VBUSパターンのみ手を入れられないが、短距離＋2.5Aクランプ＋断続運用で許容。完全に消すなら将来パスBでUSB-Cコネクタ基板直付け（その場合CC1/CC2に5.1kプルダウン要）。

## 14.10 成立性確認（データシート照合）

全アクティブ部品をデータシート照合し、設計成立を確認済み（△はXIAO 5Vピンのみ＝公表最大電流なし・実測/運用でカバー）。

| 部品 | 重要スペック | 本設計の条件と検証 | 判定 |
|---|---|---|:--:|
| MTP4835I3(P-MOS) | BVDSS −30V / Rds=27mΩ@Vgs=−4.5V / 18mΩ@−10V | 母線5Vで耐圧6倍余裕。高側ゲートはRpu10k/Rg1k分圧で0.45V＝Vgs≈−4.55V→Rds≈27mΩ、3Aで降下81mV・損失0.24W。ボディダイオード向きも高側で正 | ◎ |
| 2N7000(ゲート駆動) | Vgs(th) max 3V | GPIO3.3V駆動で十分オン。P-MOSゲートを0.45Vまでシンク（0.5mA級）。ソフトスタートτ=(Rg∥Rpu)×Cgs≈1〜9ms | ◎ |
| TC74AC125P | VCC 2.0–5.5V / VIH=0.7×VCC / VOH≈VCC / tPD 5ns | VCC=4.4V(1N4148で−0.6V)→VIH≈3.08V＜3.3V入力 ✓／出力High≈4.4V＞WS2812B VIH3.5V ✓／5ns≪1.25µs(800kHz) ✓ | ◎ |
| 1N4148 | Vf≈0.6V@数mA | AC125動的ICC(1〜3mA)でVf0.55–0.62V→VCC4.4–4.5V着地（VIH3.08–3.19V、3.3V以下）狙い通り | ◎ |
| WS2812B | VIH=0.7×VDD=3.5V@5V / 800kHz | AC125出力4.4V＞3.5V。母線がVdropで4.7Vでも VIH3.29V で余裕 | ◎ |
| XIAO 5Vピン | 公表最大電流なし。回路図上**VBUS直タップ・直列制限素子なし**（U3/U4は並列枝） | 律速は機械・熱のみ。ヘッダ直ハンダ＋スズメッキ線φ0.6mm以上で単一5V/GNDピン(≈3A定格)内。2.5Aクランプ＋断続で実用可。ソケット接点を作らないのが条件 | ○※ |

**微調整メモ**：P-MOSゲートは分圧で−4.55V（Rds規定点−4.5Vちょうどで全開・問題なし）。余裕が欲しければ Rpu 10k→47k で Vgs≈−4.9V に（ソフトスタートτはほぼ不変）。

参照データシート：MTP4835系（alltransistors/CYStek）、TC74AC125（Toshiba/onsemi 74AC125）、2N7000（各社）、WS2812B（Worldsemi）、XIAO ESP32-S3（Seeed Wiki）。